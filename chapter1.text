Hello World !

git init => Track what happens in this folder

Committing a file is synonymous with taking a snapshot of the state of a file or backing up a file

Git add 'xxx' => tell Git that should track the change we make to it